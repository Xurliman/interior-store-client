<button class="camera__item d-flex flex-column" data-view="{{ $view->name }}">
    <img data-view="{{ $view->name }}" class="camera__item_img {{ $view->name }}" src="" alt="" />
    <span class="camera__item_txt">{{ $view->name }}</span>
</button>
